[metadata]
name = "files"
description = "A channel to select files and directories"
requirements = ["fd", "bat"]

[source]
command = ['fd -tf -tl', 'fd -tf -tl -H']

[preview]
command = "bat -n --color=always '{}'"
env = { BAT_THEME = "ansi" }
